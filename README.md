# Scribe frontend module boilerplate

## Installation
clone repository, install the dependencies and build.

```sh
git clone https://github.com/scribe-systems-sp/frontend_module_boilerplate.git module
cd module
npm install
npm run build
```